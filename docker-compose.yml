services:
  frameflow:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env.example
    ports:
      - "8000:8000"
    volumes:
      - ./frameflow-data:/app/frameflow-data
    restart: unless-stopped
