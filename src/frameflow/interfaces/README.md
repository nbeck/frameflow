# Interfaces

User-facing and runtime entry points live here.

This package may contain the HTTP API, CLI commands, scheduled workers, or other delivery
mechanisms. Interface code should stay thin and delegate real behavior to the application layer.
