# DAKboard Client

DAKboard-specific behavior lives here.

This package should adapt FrameFlow output into formats and endpoints that DAKboard can
consume, while keeping DAKboard-specific assumptions out of the domain layer.
