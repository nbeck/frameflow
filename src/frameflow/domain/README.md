# Domain Layer

Core FrameFlow business concepts live here.

This package should contain pure, dependency-light code for concepts such as photos,
albums, display targets, rotation rules, and selection history.

The domain layer must not depend on application, infrastructure, client, or interface code.
