# Infrastructure Layer

Concrete technical implementations live here.

This package is responsible for adapters such as filesystem access, metadata persistence,
configuration loading, image processing integrations, and external service integrations.

Infrastructure code should depend inward on domain and application contracts.
